Sources for the plugin `plugin.appodeal.IronSource`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.IronSource" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
